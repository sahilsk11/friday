# Opencode HTTP endpoint
OPENCODE_BASE_URL=http://localhost:4096

# STT/TTS (cloud providers for v1)
DEEPGRAM_API_KEY=
CARTESIA_API_KEY=

# Tool narration — cheap model via OpenRouter for contextual descriptions.
# Without this key the static fallback phrases from narration_policy are used.
OPENROUTER_API_KEY=

# App
FRIDAY_DB_PATH=./friday.db
FRIDAY_AUTH_TOKEN=
