"""``friday`` CLI — manage and attach to opencode sessions from the terminal.

Subcommands:
- ``friday list``               list sessions with last-activity
- ``friday new [--title T]``    create a new opencode session
- ``friday attach <id>``        stream events for one session into stdout
"""
